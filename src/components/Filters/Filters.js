import React from "react";
import styles from "./Filters.scss";

const Filters = () => (
  <div className={styles.filters}>
    <h3 className={styles.filters_title}>Filters Section/Dropdowns</h3>
  </div>
);

export default Filters;
